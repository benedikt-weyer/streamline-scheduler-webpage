apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: planera-webpage-staging
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: webpage-config
  namespace: planera-webpage-staging
data:
  POSTGRES_USER: webpage
  POSTGRES_DB: webpage_db
  NODE_ENV: production
---
apiVersion: v1
kind: Secret
metadata:
  name: webpage-secrets
  namespace: planera-webpage-staging
type: Opaque
stringData:
  POSTGRES_PASSWORD: changeme-staging
  DATABASE_URL: postgresql://webpage:changeme-staging@postgres:5432/webpage_db
  BETTER_AUTH_SECRET: changeme-staging-better-auth-secret-at-least-32-chars
  STRIPE_SECRET_KEY: sk_test_changeme
  STRIPE_WEBHOOK_SECRET: whsec_changeme
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: planera-webpage-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: webpage-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: webpage-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: webpage-config
              key: POSTGRES_DB
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - webpage
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - webpage
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: planera-webpage-staging
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None

