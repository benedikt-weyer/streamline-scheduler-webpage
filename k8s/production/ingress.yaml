apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webpage-ingress
  namespace: plandera-webpage-production
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
  # New domain - plandera.com (with and without www)
  - host: __PRODUCTION_DOMAIN_WWW__
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: webpage
            port:
              number: 3000
  - host: __PRODUCTION_DOMAIN__
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: webpage
            port:
              number: 3000
  # Old domain - streamline-scheduler.com (shows rebrand page)
  - host: __OLD_DOMAIN_WWW__
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: webpage
            port:
              number: 3000
  - host: __OLD_DOMAIN__
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: webpage
            port:
              number: 3000
  tls:
  - hosts:
    - __PRODUCTION_DOMAIN_WWW__
    - __PRODUCTION_DOMAIN__
    - __OLD_DOMAIN_WWW__
    - __OLD_DOMAIN__
    secretName: production-webpage-tls

